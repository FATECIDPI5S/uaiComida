//fonts.js: Aqui é onde armazeno os tamanhos de fontes utilizadas no projeto, então, assim 
//como no colors, todos tamanhos de fontes devem possuir algum significado e por isso estão nesse arquivo.

const fonts = {
  input: 16,
  regular: 14,
  medium: 12,
  small: 11,
  tiny: 10,
};
  
export default fonts;